--- origsrc/SDL-1.2.13/configure.in	2008-12-23 01:45:26.709250000 -0600
+++ src/SDL-1.2.13/configure.in	2008-12-23 01:44:56.000000000 -0600
@@ -40,7 +40,7 @@
 AC_SUBST(LT_AGE)
 
 dnl Detect the canonical build and host environments
-AC_CONFIG_AUX_DIRS($srcdir/build-scripts)
+AC_CONFIG_AUX_DIR(build-scripts)
 AC_CANONICAL_HOST
 AC_C_BIGENDIAN
 if test x$ac_cv_c_bigendian = xyes; then
@@ -60,11 +60,6 @@
     INCLUDE="-Iinclude $INCLUDE"
 fi
 case "$host" in
-    *-*-cygwin*)
-        # We build SDL on cygwin without the UNIX emulation layer
-        BASE_CFLAGS="-I/usr/include/mingw -mno-cygwin"
-        BASE_LDFLAGS="-mno-cygwin"
-        ;;
     *)
         BASE_CFLAGS="-D_GNU_SOURCE=1"
         BASE_LDFLAGS=""
@@ -131,7 +126,7 @@
 
     dnl Checks for library functions.
     case "$host" in
-    *-*-cygwin* | *-*-mingw32*)
+    *-*-mingw32*)
         ;;
     *)
         AC_FUNC_ALLOCA
@@ -452,9 +447,14 @@
             AC_ARG_ENABLE(esd-shared,
 AC_HELP_STRING([--enable-esd-shared], [dynamically load ESD audio support [[default=yes]]]),
                           , enable_esd_shared=yes)
+            case "$host" in
+                *-*-cygwin*) esd_lib='cygesd-0.dll' ;;
+                *)
             esd_lib_spec=`echo $ESD_LIBS | sed 's/.*-L\([[^ ]]*\).*/\1\/libesd.so.*/'`
             esd_lib=`ls -- $esd_lib_spec | sed 's/.*\/\(.*\)/\1/; q'`
             echo "-- $esd_lib_spec -> $esd_lib"
+                ;;
+            esac
 
             AC_DEFINE(SDL_AUDIO_DRIVER_ESD)
             SOURCES="$SOURCES $srcdir/src/audio/esd/*.c"
@@ -561,9 +561,14 @@
                 AC_ARG_ENABLE(arts-shared,
 AC_HELP_STRING([--enable-arts-shared], [dynamically load aRts audio support [[default=yes]]]),
                               , enable_arts_shared=yes)
+                case "$host" in
+                    *-*-cygwin*) arts_lib='cygartsc-0.dll' ;;
+                    *)
                 arts_lib_spec="$ARTS_PREFIX/lib/libartsc.so.*"
                 arts_lib=`ls -- $arts_lib_spec | sed 's/.*\/\(.*\)/\1/; q'`
                 echo "-- $arts_lib_spec -> $arts_lib"
+                        ;;
+                esac
 
                 AC_DEFINE(SDL_AUDIO_DRIVER_ARTS)
                 SOURCES="$SOURCES $srcdir/src/audio/arts/*.c"
@@ -697,7 +702,7 @@
 
         if test x"$NASMFLAGS" = x; then
             case $ARCH in
-              win32)
+              win32|cygwin)
                   NASMFLAGS="-f win32"
                   ;;
               openbsd)
@@ -930,6 +935,13 @@
                           , enable_x11_shared=maybe)
 
             case "$host" in
+                *-*-cygwin*)
+                    x11_symbols_private=yes
+                    x11_lib='cygX11-6.dll'
+                    x11ext_lib='cygXext-6.dll'
+                    xrender_lib='cygXrender-1.dll'
+                    xrandr_lib='cygXrandr-2.dll'
+                    ;;
                 *-*-darwin*) # Latest Mac OS X actually ships with Xrandr/Xrender libs...
                     x11_symbols_private=yes
                     x11_lib='/usr/X11R6/lib/libX11.6.dylib'
@@ -2417,7 +2429,7 @@
             have_timers=yes
         fi
         ;;
-    *-*-cygwin* | *-*-mingw32*)
+    *-*-mingw32*)
         ARCH=win32
         if test "$build" != "$host"; then # cross-compiling
             # Default cross-compile location
@@ -2489,6 +2501,44 @@
         SDL_CFLAGS="$SDL_CFLAGS -Dmain=SDL_main"
         SDL_LIBS="-lmingw32 -lSDLmain $SDL_LIBS -mwindows"
         ;;
+    *-*-cygwin*)
+        ARCH=cygwin
+        CheckDummyVideo
+        CheckDiskAudio
+        CheckDummyAudio
+        CheckDLOPEN
+        CheckNASM
+        CheckOSS
+        CheckARTSC
+        CheckESD
+        CheckNAS
+        CheckX11
+        CheckGGI
+        CheckAAlib
+        CheckCaca
+        CheckOpenGLX11
+        CheckPTHREAD
+        # Set up files for the joystick library
+        if test x$enable_joystick = xyes; then
+            AC_DEFINE(SDL_JOYSTICK_WINMM)
+            SOURCES="$SOURCES $srcdir/src/joystick/win32/*.c"
+            have_joystick=yes
+        fi
+        # Set up files for the cdrom library
+        if test x$enable_cdrom = xyes; then
+            AC_DEFINE(SDL_CDROM_WIN32)
+            SOURCES="$SOURCES $srcdir/src/cdrom/win32/*.c"
+            have_cdrom=yes
+        fi
+        # Set up files for the timer library
+        if test x$enable_timers = xyes; then
+            AC_DEFINE(SDL_TIMER_UNIX)
+            SOURCES="$SOURCES $srcdir/src/timer/unix/*.c"
+            have_timers=yes
+        fi
+        # Set up the system libraries we need
+        EXTRA_LDFLAGS="$EXTRA_LDFLAGS -lwinmm"
+        ;;
     *-*-beos*)
         ARCH=beos
         ac_default_prefix=/boot/develop/tools/gnupro
--- origsrc/SDL-1.2.13/include/SDL_platform.h	2008-12-23 01:45:26.740500000 -0600
+++ src/SDL-1.2.13/include/SDL_platform.h	2008-12-23 01:44:56.000000000 -0600
@@ -37,6 +37,10 @@
 #undef __BSDI__
 #define __BSDI__	1
 #endif
+#if defined(__CYGWIN__)
+#undef __CYGWIN__
+#define __CYGWIN__	1
+#endif
 #if defined(_arch_dreamcast)
 #undef __DREAMCAST__
 #define __DREAMCAST__	1
--- origsrc/SDL-1.2.13/src/video/x11/SDL_x11gl.c	2008-12-23 01:45:26.756125000 -0600
+++ src/SDL-1.2.13/src/video/x11/SDL_x11gl.c	2008-12-23 01:44:56.000000000 -0600
@@ -35,6 +35,8 @@
 #define DEFAULT_OPENGL	"libGL.so.3"
 #elif defined(__OpenBSD__)
 #define DEFAULT_OPENGL	"libGL.so.4.0"
+#elif defined(__CYGWIN__)
+#define DEFAULT_OPENGL	"cygGL-1.dll"
 #else
 #define DEFAULT_OPENGL	"libGL.so.1"
 #endif
